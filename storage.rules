rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Mengizinkan user yang sudah LOGIN untuk upload dan melihat foto.
      // Wajib diterapkan agar upload foto profil berhasil.
      allow read, write: if request.auth != null;
    }
  }
}
