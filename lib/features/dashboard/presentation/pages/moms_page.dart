import 'package:flutter/material.dart';

class MomsPage extends StatelessWidget {
  const MomsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Mom's Health")),
      body: const Center(child: Text("Mom's Health Feature Coming Soon")),
    );
  }
}
